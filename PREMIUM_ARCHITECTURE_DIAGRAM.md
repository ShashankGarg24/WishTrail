# Premium System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           WishTrail Premium System                       │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐│
│  │                         User Action                                 ││
│  │  "User clicks 'Create Goal' button"                                ││
│  └─────────────────────────┬──────────────────────────────────────────┘│
│                            │                                             │
│                            ▼                                             │
│  ┌────────────────────────────────────────────────────────────────────┐│
│  │                    FRONTEND LAYER (UX Only)                        ││
│  │                                                                     ││
│  │  ┌──────────────────────────────────────────────────────────────┐ ││
│  │  │ React Component: GoalCreationForm                           │ ││
│  │  │  - Import: useGoalLimits(currentGoals)                     │ ││
│  │  │  - Check: goalLimits.canCreate                             │ ││
│  │  │  - If false: Show UpgradeModal                             │ ││
│  │  │  - If true: Send API request                               │ ││
│  │  └──────────────────────────────────────────────────────────────┘ ││
│  │                            │                                        ││
│  │  ┌──────────────────────────────────────────────────────────────┐ ││
│  │  │ Hook: useGoalLimits                                         │ ││
│  │  │  - Gets user.premium_expires_at from Redux                 │ ││
│  │  │  - Calls: getFeatureLimits('goals', premium_expires_at)   │ ││
│  │  │  - Returns: { canCreate, maxGoals, remaining, ... }       │ ││
│  │  └──────────────────────────────────────────────────────────────┘ ││
│  │                            │                                        ││
│  │  ┌──────────────────────────────────────────────────────────────┐ ││
│  │  │ Config: premiumFeatures.js                                  │ ││
│  │  │  - PREMIUM_FEATURES = { goals: { free: {...}, premium: {...}}}││
│  │  │  - isPremiumActive(premium_expires_at)                     │ ││
│  │  │  - ⚠️ WARNING: UI ONLY - Server validates!                 │ ││
│  │  └──────────────────────────────────────────────────────────────┘ ││
│  └─────────────────────────┬──────────────────────────────────────────┘│
│                            │                                             │
│                            │ POST /api/goals                             │
│                            │ { title, description, subgoals }            │
│                            │                                             │
│                            ▼                                             │
│  ┌────────────────────────────────────────────────────────────────────┐│
│  │                     BACKEND LAYER (Security)                       ││
│  │                                                                     ││
│  │  ┌──────────────────────────────────────────────────────────────┐ ││
│  │  │ Middleware: auth.js                                         │ ││
│  │  │  - Verify JWT token                                         │ ││
│  │  │  - Decode userId                                            │ ││
│  │  │  - Attach req.user                                          │ ││
│  │  └───────────────────┬──────────────────────────────────────────┘ ││
│  │                      │                                             ││
│  │                      ▼                                             ││
│  │  ┌──────────────────────────────────────────────────────────────┐ ││
│  │  │ Middleware: premium.js (attachPremiumInfo)                  │ ││
│  │  │  - Query: SELECT premium_expires_at FROM users WHERE id=?  │ ││
│  │  │  - Calculate: isPremium = expiresAt > NOW()                │ ││
│  │  │  - Attach: req.premium = { isPremium, expiresAt, ... }     │ ││
│  │  └───────────────────┬──────────────────────────────────────────┘ ││
│  │                      │                                             ││
│  │                      ▼                                             ││
│  │  ┌──────────────────────────────────────────────────────────────┐ ││
│  │  │ Controller: goalController.js (createGoal)                  │ ││
│  │  │                                                              │ ││
│  │  │  1. Validate input                                          │ ││
│  │  │                                                              │ ││
│  │  │  2. Call: validateGoalCreation(userId, req.premium)        │ ││
│  │  │     ┌──────────────────────────────────────────────┐       │ ││
│  │  │     │ Utility: premiumEnforcement.js              │       │ ││
│  │  │     │  - Count current: countActiveGoals(userId)  │       │ ││
│  │  │     │  - Get limit: getFeatureLimits('goals')     │       │ ││
│  │  │     │  - Compare: current >= limit?               │       │ ││
│  │  │     │  - Return: { allowed, error, message }      │       │ ││
│  │  │     └──────────────────────────────────────────────┘       │ ││
│  │  │                                                              │ ││
│  │  │  3. If !allowed: return 403 {                              │ ││
│  │  │       requiresPremium: true,                                │ ││
│  │  │       upgradeUrl: '/premium',                               │ ││
│  │  │       limit: 5, current: 5                                  │ ││
│  │  │     }                                                        │ ││
│  │  │                                                              │ ││
│  │  │  4. If allowed: Create goal in database                    │ ││
│  │  └───────────────────┬──────────────────────────────────────────┘ ││
│  │                      │                                             ││
│  │                      ▼                                             ││
│  │  ┌──────────────────────────────────────────────────────────────┐ ││
│  │  │ Service: pgGoalService.js                                   │ ││
│  │  │                                                              │ ││
│  │  │  countActiveGoals(userId):                                  │ ││
│  │  │    SELECT COUNT(*) FROM goals                               │ ││
│  │  │    WHERE user_id = ? AND is_active = true                   │ ││
│  │  │    AND completed = false                                    │ ││
│  │  │                                                              │ ││
│  │  │  createGoal(data):                                          │ ││
│  │  │    INSERT INTO goals (...) VALUES (...)                     │ ││
│  │  └───────────────────┬──────────────────────────────────────────┘ ││
│  │                      │                                             ││
│  └──────────────────────┼─────────────────────────────────────────────┘│
│                         │                                              │
│                         ▼                                              │
│  ┌────────────────────────────────────────────────────────────────────┐│
│  │                       DATABASE LAYER                               ││
│  │                                                                     ││
│  │  ┌──────────────────────────────────────────────────────────────┐ ││
│  │  │ Table: users                                                │ ││
│  │  │ ┌────┬───────┬─────────────────────────┐                   │ ││
│  │  │ │ id │ email │ premium_expires_at      │                   │ ││
│  │  │ ├────┼───────┼─────────────────────────┤                   │ ││
│  │  │ │ 1  │ free  │ NULL                    │ ← Free user       │ ││
│  │  │ │ 2  │ prem  │ 2025-12-31 23:59:59    │ ← Premium user    │ ││
│  │  │ │ 3  │ exp   │ 2024-01-01 00:00:00    │ ← Expired         │ ││
│  │  │ └────┴───────┴─────────────────────────┘                   │ ││
│  │  │                                                              │ ││
│  │  │ Index: idx_premium_expires ON (premium_expires_at)         │ ││
│  │  └──────────────────────────────────────────────────────────────┘ ││
│  │                                                                     ││
│  │  ┌──────────────────────────────────────────────────────────────┐ ││
│  │  │ Table: goals                                                │ ││
│  │  │ ┌────┬─────────┬────────┬─────────────┬───────────┐        │ ││
│  │  │ │ id │ user_id │ title  │ is_active   │ completed │        │ ││
│  │  │ ├────┼─────────┼────────┼─────────────┼───────────┤        │ ││
│  │  │ │ 1  │ 1       │ Goal 1 │ true        │ false     │        │ ││
│  │  │ │ 2  │ 1       │ Goal 2 │ true        │ false     │        │ ││
│  │  │ │ 3  │ 1       │ Goal 3 │ true        │ false     │        │ ││
│  │  │ │ 4  │ 1       │ Goal 4 │ true        │ false     │        │ ││
│  │  │ │ 5  │ 1       │ Goal 5 │ true        │ false     │ ← At limit││
│  │  │ └────┴─────────┴────────┴─────────────┴───────────┘        │ ││
│  │  │                                                              │ ││
│  │  │ Query: SELECT COUNT(*) = 5 ← User at free limit (5)        │ ││
│  │  └──────────────────────────────────────────────────────────────┘ ││
│  └─────────────────────────────────────────────────────────────────────┘│
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐│
│  │                         RESPONSE FLOW                              ││
│  │                                                                     ││
│  │  Backend returns 403:                                              ││
│  │  {                                                                  ││
│  │    "error": "Goal limit reached",                                  ││
│  │    "requiresPremium": true,                                        ││
│  │    "upgradeUrl": "/premium",                                       ││
│  │    "current": 5,                                                   ││
│  │    "limit": 5                                                      ││
│  │  }                                                                  ││
│  │                                                                     ││
│  │  Frontend catches 403:                                             ││
│  │  if (response.status === 403 && data.requiresPremium) {           ││
│  │    setShowUpgradeModal(true); // Show upgrade modal               ││
│  │  }                                                                  ││
│  │                                                                     ││
│  │  User sees:                                                         ││
│  │  ┌────────────────────────────────────────────────────────────┐  ││
│  │  │           Upgrade to Premium                              │  ││
│  │  │                                                            │  ││
│  │  │  You've reached your limit. Upgrade to premium for        │  ││
│  │  │  unlimited access.                                         │  ││
│  │  │                                                            │  ││
│  │  │  Current Plan: 5 goals  →  Premium Plan: 10 goals        │  ││
│  │  │                                                            │  ││
│  │  │  [Maybe Later]  [Upgrade Now]                             │  ││
│  │  └────────────────────────────────────────────────────────────┘  ││
│  └─────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════
                          SECURITY LAYERS
═══════════════════════════════════════════════════════════════════════════

Layer 1: Frontend (UX)
┌──────────────────────────────────────────────────────────────────┐
│ ✓ Shows limits before action                                     │
│ ✓ Disables buttons at limit                                      │
│ ✓ Displays progress bars                                         │
│ ✓ Shows upgrade prompts                                          │
│ ⚠️  BYPASSED BY: Modified JavaScript, browser DevTools          │
│ 🎯 PURPOSE: Improve user experience, reduce API calls           │
└──────────────────────────────────────────────────────────────────┘
                              ↓ User can bypass
                              
Layer 2: Backend Authentication
┌──────────────────────────────────────────────────────────────────┐
│ ✓ Verifies JWT token                                            │
│ ✓ Validates user identity                                       │
│ ⚠️  BYPASSED BY: Stolen/leaked token                            │
│ 🎯 PURPOSE: Ensure request comes from authenticated user        │
└──────────────────────────────────────────────────────────────────┘
                              ↓ Token verified
                              
Layer 3: Backend Premium Middleware
┌──────────────────────────────────────────────────────────────────┐
│ ✓ Queries database for premium_expires_at                       │
│ ✓ Calculates isPremium = expiresAt > NOW()                      │
│ ✓ Attaches premium info to request                              │
│ ⚠️  BYPASSED BY: Database compromise (infrastructure level)     │
│ 🎯 PURPOSE: Single source of truth for premium status           │
└──────────────────────────────────────────────────────────────────┘
                              ↓ Premium status known
                              
Layer 4: Backend Controller Validation
┌──────────────────────────────────────────────────────────────────┐
│ ✓ Queries database: COUNT(*) current usage                      │
│ ✓ Gets limit from config based on premium status                │
│ ✓ Compares: current >= limit?                                   │
│ ✓ Returns 403 if exceeded                                       │
│ ⚠️  CANNOT BE BYPASSED (without database access)                │
│ 🎯 PURPOSE: Actual enforcement, security layer                  │
└──────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════
                      WHY IT'S HARD TO BYPASS
═══════════════════════════════════════════════════════════════════════════

❌ Can't Modify JavaScript
   Frontend checks are UX only. Backend always validates.

❌ Can't Send Fake Counts
   Backend queries database directly: SELECT COUNT(*) FROM goals

❌ Can't Tamper with JWT
   Premium status not in JWT. Queried from database each request.

❌ Can't Modify Redux State
   Backend doesn't trust client state. Always queries database.

❌ Can't Bypass API
   All actions go through API. No direct database access from client.

✅ Only Way to Bypass
   1. Compromise database server (infrastructure security)
   2. Find SQL injection vulnerability (prevented by parameterized queries)
   3. Steal admin credentials (access control)

═══════════════════════════════════════════════════════════════════════════
                         DATA FLOW EXAMPLE
═══════════════════════════════════════════════════════════════════════════

Free User (user_id: 1) at Limit:

1. Frontend:
   currentGoals = 5 (from API fetch)
   useGoalLimits(5) → canCreate = false
   Shows: "Limit reached (5/5)"

2. User bypasses frontend (modifies JS to enable button):
   Sends: POST /api/goals { title: "New Goal" }

3. Backend:
   Auth middleware → userId = 1 ✓
   Premium middleware → 
     Query: SELECT premium_expires_at FROM users WHERE id = 1
     Result: NULL
     isPremium = false ✓
   
   Controller → 
     Query: SELECT COUNT(*) FROM goals WHERE user_id = 1 AND is_active = true
     Result: 5
     Limit: 5 (free tier)
     5 >= 5 → REJECT ✗
   
   Response: 403 {
     error: "Goal limit reached",
     requiresPremium: true,
     current: 5,
     limit: 5
   }

4. Frontend:
   Catches 403 → Shows upgrade modal

Result: ✅ User cannot bypass limit

═══════════════════════════════════════════════════════════════════════════
```
